import {Component,inject} from '@angular/core';import {CommonModule} from '@angular/common';import {MatCardModule} from '@angular/material/card';import {MatChipsModule} from '@angular/material/chips';import {OrderService} from '../../core/services';
@Component({standalone:true,imports:[CommonModule,MatCardModule,MatChipsModule],template:`<div class="page narrow"><div class="page-title"><span class="eyebrow">ACCOUNT</span><h1>Your orders</h1></div><mat-card class="order-card" *ngFor="let o of orders"><div><b>{{o.order_number}}</b><span>₹{{o.total}}</span></div><div><mat-chip>{{o.order_status}}</mat-chip><small>{{o.payment_status}}</small></div></mat-card><div *ngIf="!orders.length" class="empty"><h2>No orders yet</h2></div></div>`})
export class OrdersComponent{orders:any[]=[];o=inject(OrderService);constructor(){this.o.list().subscribe(r=>this.orders=r.data||[])}}
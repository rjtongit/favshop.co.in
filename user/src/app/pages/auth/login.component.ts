import {Component,inject} from '@angular/core';import {CommonModule} from '@angular/common';import {FormsModule} from '@angular/forms';import {Router,RouterLink} from '@angular/router';import {MatFormFieldModule} from '@angular/material/form-field';import {MatInputModule} from '@angular/material/input';import {MatButtonModule} from '@angular/material/button';import {MatIconModule} from '@angular/material/icon';import {AuthService} from '../../core/services';
@Component({standalone:true,imports:[CommonModule,FormsModule,RouterLink,MatFormFieldModule,MatInputModule,MatButtonModule,MatIconModule],template:`<div class="auth-page"><div class="auth-card"><div class="auth-logo">ॐ</div><span class="eyebrow">WELCOME BACK</span><h1>Sign in</h1><p>Continue to your FavShop account.</p><mat-form-field appearance="outline"><mat-label>Email</mat-label><input matInput type="email" [(ngModel)]="email"></mat-form-field><mat-form-field appearance="outline"><mat-label>Password</mat-label><input matInput type="password" [(ngModel)]="password"></mat-form-field><button mat-flat-button class="primary-btn wide" (click)="go()">Sign in</button><p class="error">{{error}}</p><div class="auth-foot">New to FavShop? <a routerLink="/register">Create an account</a></div></div></div>`})
export class LoginComponent{email='';password='';error='';a=inject(AuthService);r=inject(Router);go(){this.a.login({email:this.email,password:this.password}).subscribe({next:x=>{this.a.save(x);this.r.navigateByUrl('/products')},error:e=>this.error=e.error?.detail||'Unable to sign in'})}}
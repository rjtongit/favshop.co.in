import {Component,inject} from '@angular/core';
import {CommonModule} from '@angular/common';import {MatTableModule} from '@angular/material/table';import {MatButtonModule} from '@angular/material/button';import {MatIconModule} from '@angular/material/icon';import {MatCardModule} from '@angular/material/card';import {MatChipsModule} from '@angular/material/chips';import {AdminApiService} from '../../core/services';
@Component({standalone:true,imports:[CommonModule,MatTableModule,MatButtonModule,MatIconModule,MatCardModule,MatChipsModule],template:`<div class="page-head"><div><span class="eyebrow">CUSTOMER VOICE</span><h1>Reviews</h1><p>Manage your reviews from one workspace.</p></div></div><mat-card class="table-card"><div class="table-toolbar"><b>All Reviews</b><span class="spacer"></span><span>{{items.length}} records</span></div><div class="empty-management" *ngIf="!items.length"><mat-icon>star</mat-icon><h3>No records available</h3><p>The FastAPI endpoint can populate this management view.</p></div><div class="table-wrap" *ngIf="items.length"><table mat-table [dataSource]="items"><ng-container matColumnDef="id"><th mat-header-cell *matHeaderCellDef>ID</th><td mat-cell *matCellDef="let x">{{x.id}}</td></ng-container><ng-container matColumnDef="name"><th mat-header-cell *matHeaderCellDef>NAME</th><td mat-cell *matCellDef="let x">{{x.name||x.order_number||x.email||'—'}}</td></ng-container><ng-container matColumnDef="status"><th mat-header-cell *matHeaderCellDef>STATUS</th><td mat-cell *matCellDef="let x"><mat-chip>{{x.status||x.order_status||'Active'}}</mat-chip></td></ng-container><tr mat-header-row *matHeaderRowDef="cols"></tr><tr mat-row *matRowDef="let row;columns:cols"></tr></table></div></mat-card>`})
export class ReviewsComponent{api=inject(AdminApiService);items:any[]=[];cols=['id','name','status'];constructor(){this.api.reviews().subscribe({next:r=>this.items=r.data||[],error:()=>this.items=[]})}}